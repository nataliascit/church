/**
 * @desc this class will hold functions used to execute HTTP requests to retrieve or manage the content of a Bead.
 * @author wcustodio
 * @required $resource
 */
(function(){
    'use strict';

    angular.module('paymentManagement', ['$resource']).service('beadRestService', function ($resource) {
    });
})();