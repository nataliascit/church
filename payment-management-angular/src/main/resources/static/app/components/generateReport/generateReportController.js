import './app.generateReport.module';

/**
 * @desc This Controller is responsible for handling the view 'generateReportView.html'
 * @author wcustodio
 */
(function () {
    'use strict';

    var generateReportModule = angular.module('paymentManagement.generateReport');

    generateReportModule.$inject = [];

    generateReportModule.controller('generateReportController', function () {
    });
}());
